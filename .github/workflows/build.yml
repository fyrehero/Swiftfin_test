name: Build Swiftfin

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    name: Build iOS IPA
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Install build tools
        run: brew install carthage swiftgen swiftformat swiftlint

      - name: Cache Carthage
        uses: actions/cache@v4
        with:
          path: Carthage
          key: ${{ runner.os }}-carthage-${{ hashFiles('Cartfile.resolved') }}
          restore-keys: |
            ${{ runner.os }}-carthage-

      - name: Install Carthage dependencies
        run: carthage update --use-xcframeworks --platform iOS

      - name: Create xcconfig (no signing)
        run: |
          mkdir -p XcodeConfig
          cat > XcodeConfig/DevelopmentTeam.xcconfig << 'EOF'
          DEVELOPMENT_TEAM = ""
          PRODUCT_BUNDLE_IDENTIFIER = org.jellyfin.swiftfin
          EOF

      - name: Build (unsigned)
        run: |
          xcodebuild \
            -project Swiftfin.xcodeproj \
            -scheme "Swiftfin" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -derivedDataPath /tmp/DerivedData \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            AD_HOC_CODE_SIGNING_ALLOWED=YES

      - name: Package as IPA
        run: |
          APP_PATH=$(find /tmp/DerivedData -name "*.app" -not -path "*/SourcePackages/*" | head -n 1)
          echo "Found app at: $APP_PATH"
          mkdir -p Payload
          cp -r "$APP_PATH" Payload/
          zip -r Swiftfin.ipa Payload

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: Swiftfin-unsigned
          path: Swiftfin.ipa
          retention-days: 7
